<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Lion Mart</title>
  </head>
  <body>
    <h2>Details of the product</h2>
    <ul id="details">
      <li>
        Title:
        <%= @post.title %>
      </li>
      <li>
        Price:
        <%= @post.price %>
      </li>
      <li>
        Post By:
        <%= @post.post_by %>
        <%= link_to 'see his/her posts', user_posts_path(@post.post_by), :class => 'btn btn-primary col-2' %>
      </li>
      <li>
        Email:
        <%= @email %>
      </li>
      <li>
        Description:
        <%= @post.description %>
      </li>
      <% if @post.avatar.attached? %>
        <p>
          <image src="<%= url_for(@post.avatar) %>" width="20%" height="auto"/>
        </p>
        <!-- <p>-->
        <%#= image_tag @post.avatar.variant(resize: "250x250") %>
          <!-- </p>-->
        <% end %>

      </ul>

      <div class="row">
        <%if @current_user == @post.post_by %>
          <%= link_to 'Edit', edit_post_path(@post), :class => 'btn btn-primary col-2' %>
          <%= link_to 'Delete', post_path(@post), 'data-method' => 'delete', 'data-confirm' => 'Are you sure?', :class => 'btn btn-danger col-2' %>
        <%end %>
        <%= link_to 'Back to post list', posts_path, :class => 'btn btn-primary col-2' %>
      </div>
    </body>
  </html>
